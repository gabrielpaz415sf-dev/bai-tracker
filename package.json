{
  "name": "bai-tracker",
  "private": true,
  "version": "0.1.0",
  "description": "Tracker + performance attribution + entry-price education tool for BAI (iShares A.I. Innovation and Tech Active ETF)",
  "type": "module",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm:dev:server\" \"npm:dev:web\"",
    "dev:server": "npm -w server run dev",
    "dev:web": "npm -w web run dev",
    "build": "npm -w server run build && npm -w web run build",
    "start": "npm -w server run start",
    "test": "npm -w server run test",
    "typecheck": "npm -w server run typecheck && npm -w web run typecheck",
    "refresh": "npm -w server run refresh",
    "brief": "npm -w server run brief",
    "snapshot": "npm -w server run snapshot",
    "build:static": "npm run snapshot && npm -w web run build:static"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
